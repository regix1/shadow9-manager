# Core dependencies
asyncio-socks>=0.7.1
aiohttp>=3.9.0
aiohttp-socks>=0.8.4
PySocks>=1.7.1

# Security
cryptography>=41.0.0
argon2-cffi>=23.1.0
# secrets is built-in, no install needed

# Configuration
pyyaml>=6.0.1
python-dotenv>=1.0.0

# CLI
click>=8.1.0
rich>=13.7.0

# Logging
structlog>=24.1.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
